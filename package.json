{
  "name": "write-json",
  "version": "1.0.0",
  "description": "This writes the JSON data gotten from the API to a file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Oyin Dawodu",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
